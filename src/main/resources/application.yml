spring:
  application:
    name: kafka-demo
  kafka:
    bootstrap-servers: 'localhost:9092'
    consumer:
      group-id: events
  threads:
    virtual:
      enabled: true
erickson:
  kafka:
    topic: customer.visit
